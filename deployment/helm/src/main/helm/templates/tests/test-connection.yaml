apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mrmat-hello-kotlin.fullname" . }}-test-connection"
  labels:
    {{- include "mrmat-hello-kotlin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mrmat-hello-kotlin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
